This is where the module updates will be kept rather than implementing in install file.
The naming convention for the scripts will be as follows:

script_000x.inc

Where x is the update integer. Each update will increment by 1.
